runtime: nodejs
env: flex

actions:
  promote_all_traffic: true
  stop_previous_version: true

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10

env_variables:
  NODE_ENV: "production"

beta_settings:
  cloud_sql_instances: coreable:us-central1:coreable